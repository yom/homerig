#!/bin/bash
# Invoked by gpclient (running as root via sudo); derive the desktop user from SUDO_USER.
# --no-sandbox is required because the sudo environment has no browser sandbox.
TARGET_USER="${SUDO_USER:-$USER}"
TARGET_HOME=$(eval echo "~$TARGET_USER")
sudo -u "$TARGET_USER" DISPLAY="${DISPLAY:-:0}" XAUTHORITY="$TARGET_HOME/.Xauthority" \
    /usr/bin/google-chrome --profile-directory=Default --no-sandbox "$1" &
