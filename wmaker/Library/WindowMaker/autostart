#!/bin/sh
xset m 20/10 4
xset b off # disable system beep
xset s 300 5 # 5 min idle -> activate lock w/ 5s blank grace
#compton -c -r 12 -l -15 -t -15 -G -b -I 0.028 -O 0.03 --backend glx --paint-on-overlay
xss-lock --transfer-sleep-lock -- xsecurelock &
