# Allow the dotfiles user to flush tun0 routes on VPN disconnect without a password prompt.
# USERNAME is replaced by install.sh with the actual login name.
USERNAME ALL=(root) NOPASSWD: /usr/bin/ip route flush dev tun0
